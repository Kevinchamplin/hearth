# Rust build output
src-tauri/target/
# Tauri-generated schemas/config (regenerated on build)
src-tauri/gen/
# Node (only the pinned Tauri CLI lives here)
node_modules/
# Build artifacts
*.dmg
# Bundled Ollama engine — populated locally by scripts/bundle-ollama.sh before `tauri build`.
# Third-party redistributable binaries; not committed to the repo.
src-tauri/resources/ollama/
