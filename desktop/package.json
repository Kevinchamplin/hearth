{
  "name": "hearth-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Hearth desktop (Tauri + native Ollama inference). The frontend is buildless static HTML in dist/; this package only pins the Tauri CLI.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
